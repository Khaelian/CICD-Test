steps:
# get dependencies
- name: 'node:alpine'
  args: ['yarn']
# build
- name: 'node:alpine'
  args: ['yarn',  'build']
# dockerize
- name: 'docker'
  args: ['build', '-t', 'ui', '.']
# store docker image
images: ['ui']